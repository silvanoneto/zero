# Estrutura do Projeto

## 📂 Organização de Pastas

```
revolucao-cibernetica/
├── src/                    # Código-fonte TypeScript
├── dist/                   # JavaScript compilado (gerado automaticamente)
├── assets/                 # Recursos estáticos (JSON, áudio)
├── docs/                   # Documentação Markdown
├── public/                 # Arquivos públicos (manifest, SEO)
└── .backup/               # Arquivos legados (não versionado)
```

## 📋 Referências de Arquivos

### TypeScript → Assets

**src/riz∅ma-full.ts:**
- `fetch('assets/concepts.json')` → Conceitos filosóficos
- `fetch('assets/relations.json')` → Relações entre conceitos

### HTML → Assets/Public

**index.html:**
- `<link rel="manifest" href="public/manifest.json">` → PWA manifest
- `<audio src="assets/CRIO.mp3">` → Áudio de fundo

**riz∅ma.html:**
- `<script type="module" src="dist/riz∅ma-full.js">` → JavaScript compilado

### README → Docs

**README.md:**
- Badge link: `docs/CRIOS.md` → Conteúdo filosófico

## ⚠️ Importante

**Ao adicionar novos arquivos, sempre use caminhos relativos corretos:**

- Dados JSON → `assets/`
- Documentação Markdown → `docs/`
- Arquivos públicos/SEO → `public/`
- Código TypeScript → `src/`
- JavaScript compilado → `dist/` (gerado automaticamente)

**Nunca referencie arquivos sem prefixo de pasta (exceto raiz do projeto):**
- ❌ `fetch('concepts.json')`
- ✅ `fetch('assets/concepts.json')`
