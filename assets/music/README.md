Coloque aqui seu arquivo de áudio para o site CRIO.

Nome sugerido: crio-loop.mp3
Caminho no repositório: assets/music/crio-loop.mp3

Notas:
- Para respeitar navegadores, o arquivo não é auto-played sem interação do usuário. O controle 'Play' permite iniciar manualmente.
- Se preferir usar URL externa, atualize o atributo `src` de `<audio id="bg-audio">` em `index.html`.
- Licenciamento: adicione um arquivo LICENSE_AUDIO.txt se o áudio não for de sua autoria.

Exemplo (local):
- assets/music/crio-loop.mp3

Exemplo (externo):
- https://example.org/path/to/crio-loop.mp3

Se quiser que eu inclua um trecho gratuito e curto embutido (base64), solicite explicitamente e cuidarei do tamanho do arquivo.