{
  "name": "constituicao-2.0-subgraph",
  "version": "0.1.0",
  "description": "The Graph subgraph for Constituição 2.0 governance system",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ constituicao2.0",
    "remove-local": "graph remove --node http://localhost:8020/ constituicao2.0",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 constituicao2.0",
    "deploy": "graph deploy --studio constituicao-2-0",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  },
  "engines": {
    "node": ">=18.18.0",
    "npm": ">=9.0.0"
  }
}
